{
  "name": "coverage-space",
  "scripts": {
    "postdeploy": "git clone https://github.com/jacebrowning/coverage-space-data /tmp/data"
  },
  "env": {
    "FLASK_ENV": {
      "required": true
    },
    "DATA_REPO_URL": {
      "required": true
    }
  },
  "addons": [
    "papertrail"
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
