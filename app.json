{
  "stack": "heroku-24",
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  
  "env": {
    "HEROKU_APP_NAME": {
      "required": true
    },
    "FLASK_ENV": {
      "required": true
    },
    "DATA_REPO_URL": {
      "required": true
    }
  },
  "scripts": {
    "postdeploy": "make data"
  }
}
